#include <Arduino.h>

#define PIN 

void setup() {
  // pinMode(5, OUTPUT);
  // pinMode(6, OUTPUT);
  // pinMode(7, OUTPUT);
  pinMode(24, OUTPUT);
}

void loop() {
  // analogWrite(5, 100);
  // digitalWrite(7, HIGH);
  // digitalWrite(6, LOW);
  // Serial.println(digitalRead(24));
  digitalWrite()
}